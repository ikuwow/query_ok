language: ruby

sudo: false

rvm:
  - 2.2.8
  - 2.3.5
  - 2.4.2

cache: bundler

script:
  - bundle exec middleman build --verbose

deploy:
  provider: script
  skip_cleanup: true
  script: bundle exec middleman s3_sync
  on:
    branch: master

notifications:
  email: false
